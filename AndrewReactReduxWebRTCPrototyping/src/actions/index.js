export function sayHiViaWebRTC(webnode){
  console.log(webnode.address);

  //sample webrtc call saying hello

  //tasks:   1. rename this 2. 'say hi' via peer.js
  //tasks 3. initialize peer.js   4. tell broker webnode_addresses
  //  5.  make transaction request,  message back.  might need to send peer id
  // if they don't have a return value.


}
